# Share a Core Network

Creates a fabric with a create-only regional base policy and shares the Core
Network with an account through AWS RAM. The share module receives an explicit
commercial `us-east-1` provider under the role alias `aws.ram`. Replace the
example account ID before use.
