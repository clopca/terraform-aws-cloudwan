# Reference an existing fabric

References an existing Global Network and Core Network by ID only. The module
resolves ARNs, state, edges, and segments through AWS data sources and verifies
that the Core Network belongs to the resolved Global Network. Reference mode
owns no Network Manager resources.
